
eureka:
  instance:
    hostname: app2
    preferIpAddress: true
    instance-id: 127.0.0.1:${server.port}
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8000/eureka/

server:
  port: 8092
spring:
  application:
    name: sapp2